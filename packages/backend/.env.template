# OpenAI API Configuration
OPENAI_API_KEY=api_key

# Server Configuration
PORT=8000
HOST=0.0.0.0

# CORS Origins (comma-separated)
# For development: http://localhost:5173
# For production: add your Vercel domain
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Environment
ENVIRONMENT=development

# Firebase Configuration
# For Admin SDK (Backend) - Choose ONE of the following options:

# Option 1: Service Account File Path (Recommended for Development)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Option 2: Service Account JSON (Recommended for Production)
# Set the entire JSON content as environment variable (for Render/Heroku/etc)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"..."}

# Firebase Project Configuration (same as frontend)
# These should match your frontend Firebase configuration
FIREBASE_API_KEY=your-firebase-api-key
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your-sender-id
FIREBASE_APP_ID=your-app-id

